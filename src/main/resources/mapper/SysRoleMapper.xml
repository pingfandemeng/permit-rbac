<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.permit.system.role.mapper.SysRoleMapper">
    <select id="selectRolesByUserId" resultType="com.company.permit.system.role.entity.SysRole">
        SELECT r.role_id, r.role_name, r.role_key, r.data_scope, r.dept_admin_flag, r.status, r.sort
        FROM sys_role r
        INNER JOIN sys_user_role ur ON ur.role_id = r.role_id
        WHERE ur.user_id = #{userId} AND r.del_flag = '0'
    </select>
</mapper>
