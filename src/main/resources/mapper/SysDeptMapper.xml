<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.permit.system.dept.mapper.SysDeptMapper">
    <select id="selectDeptList" resultType="com.company.permit.system.dept.entity.SysDept">
        SELECT d.dept_id, d.parent_id, d.dept_name, d.dept_key, d.ancestors, d.order_num, d.status,
               d.version, d.remark, d.create_time
        FROM sys_dept d
        <where>
            d.del_flag = '0'
            <if test="status != null and status != ''">AND d.status = #{status}</if>
        </where>
        ORDER BY d.parent_id, d.order_num, d.dept_id
    </select>
    <select id="selectAllDeptsUnscoped" resultType="com.company.permit.system.dept.entity.SysDept">
        SELECT dept_id, parent_id, dept_name, dept_key, ancestors, order_num, status, version, remark
        FROM sys_dept WHERE del_flag = '0'
        ORDER BY parent_id, order_num, dept_id
    </select>
    <select id="selectChildDeptIds" resultType="long">
        SELECT dept_id FROM sys_dept
        WHERE del_flag = '0' AND FIND_IN_SET(#{deptId}, ancestors)
    </select>
    <select id="selectByKey" resultType="com.company.permit.system.dept.entity.SysDept">
        SELECT dept_id, parent_id, dept_name, dept_key, ancestors, order_num, status
        FROM sys_dept WHERE dept_key = #{deptKey} AND del_flag = '0' LIMIT 1
    </select>
</mapper>
